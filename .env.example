# Public HTTP ports
HTTP_PORT_FRONTEND=8080
HTTP_PORT_GATEWAY=3000

# Public gRPC ports
GRPC_PORT_CATALOG=50051

# JWT configuration for local development only
JWT_SECRET=local-development-secret-must-be-at-least-32-bytes
JWT_ISSUER=go-market-auth
JWT_AUDIENCE=go-market-api
