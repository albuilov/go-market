name: go-market

services:
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    environment:
      FRONTEND_HTTP_PORT: "${FRONTEND_HTTP_PORT}"
      GATEWAY_HTTP_PORT: "${GATEWAY_HTTP_PORT}"
    ports:
      - "${FRONTEND_HTTP_PORT}:${FRONTEND_HTTP_PORT}"
    depends_on:
      gateway:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--output-document=-", "http://localhost:${FRONTEND_HTTP_PORT}/healthz"]
      interval: 10s
      timeout: 3s
      retries: 3
    networks:
      - go-market-network

  gateway:
    build:
      context: .
      dockerfile: services/gateway/Dockerfile
    environment:
      GATEWAY_HTTP_ADDRESS: ":${GATEWAY_HTTP_PORT}"
      CATALOG_GRPC_ADDRESS: "catalog:${CATALOG_GRPC_PORT}"
      JWT_SECRET: "${JWT_SECRET}"
      JWT_ISSUER: "${JWT_ISSUER}"
      JWT_AUDIENCE: "${JWT_AUDIENCE}"
    ports:
      - "${GATEWAY_HTTP_PORT}:${GATEWAY_HTTP_PORT}"
    depends_on:
      catalog:
        condition: service_started
    networks:
      - go-market-network

  catalog:
    build:
      context: .
      dockerfile: services/catalog/Dockerfile
    environment:
      CATALOG_GRPC_ADDRESS: ":${CATALOG_GRPC_PORT}"
    ports:
      - "${CATALOG_GRPC_PORT}:${CATALOG_GRPC_PORT}"
    networks:
      - go-market-network

networks:
  go-market-network:
    driver: bridge
